{
  "name": "rent-workers",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "charge-generator",
    "payment-worker"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "build": "npm run build     --workspaces --if-present",
    "build:production": "npm run build --workspace charge-generator -- --env=\"\" && npm run build --workspace payment-worker -- --env=\"\"",
    "build:staging": "npm run build --workspace charge-generator -- --env staging && npm run build --workspace payment-worker -- --env staging",
    "deploy:production": "npm run deploy --workspace charge-generator -- --env=\"\" && npm run deploy --workspace payment-worker -- --env=\"\"",
    "deploy:staging": "npm run deploy --workspace charge-generator -- --env staging && npm run deploy --workspace payment-worker -- --env staging",
    "check:schema": "tsx scripts/check-airtable-schema.ts"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@typescript-eslint/eslint-plugin": "^8.8.0",
    "@typescript-eslint/parser": "^8.8.0",
    "eslint": "^9.12.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.2",
    "vitest": "^2.1.0",
    "zod": "^3.23.8"
  }
}
